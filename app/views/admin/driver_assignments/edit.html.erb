<% content_for :page_title, "Edit Driver Assignment" %>

<div class="bg-white rounded-lg shadow-lg p-6 max-w-2xl mx-auto">
  <div class="flex justify-between items-center mb-6">
    <h1 class="text-2xl sm:text-3xl font-bold text-blue-900">Edit Driver Assignment</h1>
    <%= link_to admin_driver_assignments_path, class: "text-blue-600 hover:text-blue-900 inline-flex items-center" do %>
      <svg class="w-4 h-4 mr-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18"></path>
      </svg>
      Back to Assignments
    <% end %>
  </div>

  <%= form_with(model: [:admin, @driver_assignment], local: true, class: "space-y-6") do |f| %>
    <% if @driver_assignment.errors.any? %>
      <div class="bg-red-50 border-l-4 border-red-500 rounded-lg p-4 mb-6">
        <!-- Same error display -->
      </div>
    <% end %>

    <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
      <div>
        <%= f.label :user_id, "Driver", class: "block text-sm font-medium text-gray-700 mb-2" %>
        <%= f.collection_select :user_id, @drivers, :id, :full_name, { include_blank: 'Select Driver' }, required: true, class: "w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-yellow-500 focus:border-yellow-500" %>
      </div>

      <div>
        <%= f.label :school_bus_id, "School Bus", class: "block text-sm font-medium text-gray-700 mb-2" %>
        <%= f.collection_select :school_bus_id, @school_buses, :id, :bus_number, { include_blank: 'Select Bus' }, required: true, class: "w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-yellow-500 focus:border-yellow-500" %>
      </div>

      <div>
        <%= f.label :id_type, "ID Type", class: "block text-sm font-medium text-gray-700 mb-2" %>
        <%= f.select :id_type, options_for_select(DriverAssignment::ID_TYPES.map { |k, v| [v, k] }, @driver_assignment.id_type), { include_blank: 'Select ID Type' }, required: true, class: "w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-yellow-500 focus:border-yellow-500" %>
      </div>

      <div>
        <%= f.label :id_number, "ID Number", class: "block text-sm font-medium text-gray-700 mb-2" %>
        <%= f.text_field :id_number, required: true, class: "w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-yellow-500 focus:border-yellow-500" %>
      </div>

      <div>
        <%= f.label :assigned_date, "Assigned Date", class: "block text-sm font-medium text-gray-700 mb-2" %>
        <%= f.date_field :assigned_date, required: true, class: "w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-yellow-500 focus:border-yellow-500" %>
      </div>

      <div>
        <%= f.label :status, "Status", class: "block text-sm font-medium text-gray-700 mb-2" %>
        <%= f.select :status, options_for_select(DriverAssignment::STATUSES.map { |k, v| [v, k] }, @driver_assignment.status), {}, required: true, class: "w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-yellow-500 focus:border-yellow-500" %>
      </div>

      <div class="md:col-span-2">
        <%= f.label :description, "Description / Notes (Optional)", class: "block text-sm font-medium text-gray-700 mb-2" %>
        <%= f.text_area :description, rows: 3, class: "w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-yellow-500 focus:border-yellow-500" %>
      </div>
    </div>

    <div class="flex justify-end space-x-3 pt-4 border-t">
      <%= link_to "Cancel", admin_driver_assignments_path, class: "bg-gray-500 text-white px-4 py-2 rounded hover:bg-gray-600 transition duration-150" %>
      <%= f.submit "Update Assignment", class: "bg-blue-900 text-yellow-500 px-4 py-2 rounded hover:bg-blue-800 transition duration-150 font-semibold" %>
    </div>
  <% end %>
</div>