<% content_for :page_title, "Driver Assignment Details" %>

<div class="bg-white rounded-lg shadow-lg p-6">
  <div class="flex flex-col sm:flex-row justify-between items-start sm:items-center mb-6 gap-4">
    <h1 class="text-2xl sm:text-3xl font-bold text-blue-900">Driver Assignment Details</h1>
    <div class="flex space-x-3">
      <%= link_to edit_admin_driver_assignment_path(@driver_assignment), class: "bg-yellow-500 text-blue-900 px-4 py-2 rounded hover:bg-yellow-600 transition duration-150 inline-flex items-center" do %>
        <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11 5H6a2 2 0 00-2 2v11a2 2 0 002 2h11a2 2 0 002-2v-5m-1.414-9.414a2 2 0 112.828 2.828L11.828 15H9v-2.828l8.586-8.586z"></path>
        </svg>
        Edit Assignment
      <% end %>
      <%= link_to admin_driver_assignments_path, class: "bg-gray-500 text-white px-4 py-2 rounded hover:bg-gray-600 transition duration-150 inline-flex items-center" do %>
        <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18"></path>
        </svg>
        Back to Assignments
      <% end %>
    </div>
  </div>

  <div class="grid grid-cols-1 lg:grid-cols-2 gap-6">
    <!-- Driver Information -->
    <div class="bg-gradient-to-r from-blue-50 to-white rounded-lg p-6">
      <h2 class="text-lg font-semibold text-blue-900 mb-4">Driver Information</h2>
      <div class="space-y-3">
        <div class="border-b pb-2">
          <p class="text-xs text-gray-500 uppercase tracking-wide">Driver Name</p>
          <p class="text-lg font-semibold text-gray-900"><%= @driver_assignment.driver_name %></p>
          <p class="text-xs text-gray-500 mt-1">Professional Type: <%= @driver_assignment.user&.professional_type&.humanize %></p>
        </div>
        <div class="border-b pb-2">
          <p class="text-xs text-gray-500 uppercase tracking-wide">ID Type</p>
          <p class="text-base text-gray-700"><%= @driver_assignment.id_type_humanized %></p>
        </div>
        <div class="border-b pb-2">
          <p class="text-xs text-gray-500 uppercase tracking-wide">ID Number</p>
          <p class="text-base font-mono text-gray-700"><%= @driver_assignment.id_number %></p>
        </div>
      </div>
    </div>

    <!-- Bus Information -->
    <div class="bg-gradient-to-r from-green-50 to-white rounded-lg p-6">
      <h2 class="text-lg font-semibold text-blue-900 mb-4">Bus Information</h2>
      <div class="space-y-3">
        <div class="border-b pb-2">
          <p class="text-xs text-gray-500 uppercase tracking-wide">Bus Number</p>
          <p class="text-lg font-semibold text-gray-900"><%= @driver_assignment.school_bus&.bus_number %></p>
        </div>
        <div class="border-b pb-2">
          <p class="text-xs text-gray-500 uppercase tracking-wide">Bus Model</p>
          <p class="text-base text-gray-700"><%= @driver_assignment.school_bus&.bus_model %></p>
        </div>
        <div class="border-b pb-2">
          <p class="text-xs text-gray-500 uppercase tracking-wide">Capacity</p>
          <p class="text-base text-gray-700"><%= @driver_assignment.school_bus&.capacity %> seats</p>
        </div>
      </div>
    </div>
  </div>

  <!-- Assignment Details -->
  <div class="mt-6 bg-gray-50 rounded-lg p-6">
    <h2 class="text-lg font-semibold text-blue-900 mb-4">Assignment Details</h2>
    <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
      <div>
        <p class="text-xs text-gray-500 uppercase tracking-wide">Assigned Date</p>
        <p class="text-base font-semibold text-gray-900"><%= @driver_assignment.assigned_date.strftime("%B %d, %Y") if @driver_assignment.assigned_date %></p>
      </div>
      <div>
        <p class="text-xs text-gray-500 uppercase tracking-wide">Status</p>
        <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium <%= @driver_assignment.status_color %>">
          <%= @driver_assignment.status_badge %>
        </span>
      </div>
      <div>
        <p class="text-xs text-gray-500 uppercase tracking-wide">Created On</p>
        <p class="text-base text-gray-700"><%= @driver_assignment.created_at.strftime("%B %d, %Y at %I:%M %p") %></p>
      </div>
    </div>
  </div>

  <!-- Description -->
  <% if @driver_assignment.description.present? %>
    <div class="mt-6 bg-yellow-50 rounded-lg p-6">
      <p class="text-xs text-gray-500 uppercase tracking-wide mb-2">Additional Notes</p>
      <p class="text-gray-700"><%= @driver_assignment.description %></p>
    </div>
  <% end %>

  <!-- Danger Zone -->
  <div class="mt-8 pt-6 border-t border-red-200">
    <div class="bg-red-50 rounded-lg p-6">
      <h3 class="text-lg font-semibold text-red-700 mb-3">Danger Zone</h3>
      <p class="text-sm text-red-600 mb-4">This action cannot be undone. This will permanently remove the driver assignment.</p>
      <%= link_to "Remove Assignment", admin_driver_assignment_path(@driver_assignment), method: :delete, data: { confirm: "Are you sure you want to remove this driver assignment?" }, class: "bg-red-600 text-white px-4 py-2 rounded hover:bg-red-700 transition duration-150" %>
    </div>
  </div>
</div>